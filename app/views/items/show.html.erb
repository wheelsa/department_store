<h1><%= "#{@department.name} Department" %> - <%= "#{@item.name} Page"%></h2>
<h2><%="Name: #{@item.name}"%> </h2>
 <h3><%= "Quantity: #{@item.quantity}"%></h3>
 <h3><%= "Description: #{@item.description}" %></h3>
<%= link_to 'Edit Item', edit_department_item_path([@department, @item]) %>
<%= link_to 'Delete Item', [@department, @item], method: :delete %>
<%= link_to "Back to all #{@department.name} Department Items", department_items_path(@department) %>
